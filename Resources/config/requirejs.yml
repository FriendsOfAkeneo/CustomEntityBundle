config:
    paths:
        reference-data-list: pimcustomentity/js/reference-data-list
    config:
        pim/fetcher-registry:
            fetchers:
                reference-data-list:
                    module: pim/base-fetcher
                    options:
                        urls:
                            list: pim_customentity_list
